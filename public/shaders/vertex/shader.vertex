attribute vec4 aVertexPosition;

uniform mat4 uModelViewMatrix;
uniform mat4 uProjectionMatrix;
uniform mat4 uPositionMatrix;

void main() {
    gl_Position = uProjectionMatrix * uModelViewMatrix * uPositionMatrix * aVertexPosition;
}